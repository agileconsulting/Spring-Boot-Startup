Spring boot va benissimo per creare resfull service,
piu complicato per creare un applicativo con jsp.
Infatti non avendo più il classico file system non ti ritrovi più.
Non sono riuscito ad utilizzare log4j per il logging.
Probabilmente va bene in un contesto dove un front end fatto
con angular js.
Ovviamente configurando si puo fare tutto
Lo dice anche lui:
JSPs should be avoided if possible, there are several known limitations
 when using them with embedded servlet containers